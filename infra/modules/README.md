# Terraform modules

Phase 0 defines no resource modules. Phase 1 should add only modules required by the first complete vertical slice and must preserve managed identity, disabled local authentication where supported, least privilege and the agent/executor authority split.
